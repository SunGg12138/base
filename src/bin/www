#!/usr/bin/env node

const app = require('../app');
const config = require('config');
const PORT = Number(process.env.HTTP_PORT) || config.port;

app.listen(PORT, function(){
    console.log('server run at the port:', PORT);
});